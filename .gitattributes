docs/ss_segmentation_data.csv filter=lfs diff=lfs merge=lfs -text
docs/customer_cluster_labels.csv filter=lfs diff=lfs merge=lfs -text

# Force LF for files that run inside Linux containers — prevents CRLF breakage on Windows builds
*.sh           text eol=lf
Dockerfile*    text eol=lf
docker-compose.yml text eol=lf
